Movie Recommendation System

This project recommends movies based on user preferences using collaborative filtering. It analyzes user ratings and suggests similar movies based on patterns in the dataset.

Features:
- Select a movie from the list
- Choose number of recommendations
- Get similar movie suggestions instantly
  
How to run:

pip install -r requirements.txt

python -m streamlit run recommendation_app.py
